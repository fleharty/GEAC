/target

# Project-specific config (contains local paths — not for version control)
geac.toml

# Python cache artifacts
__pycache__/
*.py[cod]

# Local reference repos cloned for comparison / inspiration.
# Kept inside the workspace for tooling access but intentionally excluded from GEAC git history.
third_party_refs/

# Private local-only notes and presentation materials.
private/

# Synthetic BAM test artifacts (regenerated by tests/synthetic_bams/generate.py)
tests/synthetic_bams/bams/
tests/synthetic_bams/*.parquet
